spring:
  profiles:
    active: ${AGENT_ENV:mysql}
  application:
    name: security-service
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:28848
      discovery:
        server-addr: 127.0.0.1:28848

